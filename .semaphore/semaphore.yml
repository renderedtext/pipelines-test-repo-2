version: v1.0
name: auto call repo
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: First block
    run:
      when: "change_in('../README.md', 'master..$SEMAPHORE_GIT_SHA')"
    task:
      jobs:
        - name: first job
          commands:
            - echo 123
  - name: Second block
    run:
      when: "change_in('/README.md', 'master...$SEMAPHORE_GIT_SHA')"
    task:
      jobs:
        - name: first job
          commands:
            - echo 123     
  - name: Third block
    run:
      when: "change_in('../.semaphore/', 'master..test')"
    task:
      jobs:
        - name: first job
          commands:
            - echo 123
