version: "v1.0"
name: Task file test
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: First block
    task:
      jobs:
        - name: first job
          commands:
            - echo "First and only command from 'pipelines-test-repo-auto-call' repo"
  - name: Block with task_file
    task_file: task.yml
